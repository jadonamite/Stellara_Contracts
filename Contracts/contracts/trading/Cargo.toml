[package]
name = "trading"
version = "0.1.0"
edition = "2021"

[features]
default = []
testutils = []

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.5.0"
shared = { path = "../../shared" }

[dev-dependencies]
soroban-sdk = { version = "20.5.0", features = ["testutils"] }
